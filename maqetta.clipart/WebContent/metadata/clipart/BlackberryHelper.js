define([
	"dojo/_base/declare",
	"./_DeviceClipartHelper"
], function(declare, _DeviceClipartHelper) {

return declare([_DeviceClipartHelper], {
	
	// These two values must match width/height attributes on <svg> element for portrait clipart
	defaultWidth:438,
	defaultHeight:794.84832

});

});
